{
  "name": "servcraftassessment-api-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Job Card API tests — Playwright + TypeScript (Part C)",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "typescript": "^5.8.0"
  }
}
